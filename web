import streamlit as st # streamlit 라이브러리 임포트

# 타이틀 텍스트 출력
st.title('첫번째 웹 어플 만들기 👋 📺 🫶 👻')

"## 이건 부제목"
"### 이건 소제목"
"#### 이건 더 작은 제목"
"""
# 비즈니스 모델 분석

[네이버](https://www.naver.com/)의 비즈니스 모델을 분석해봅시다.  
[홍익대학교](https://www.hongik.ac.kr/)의 비즈니스 모델을 분석해봅시다.

이것이 일반 본문

:red[빨간색 텍스트]:green[초록색 텍스트]:blue[파란색 텍스트]

```
import streamlit as st

print("코드 블록")
```
"""
st.caption('캡션(작고 흐릿한 텍스트):st.caption()')

with st.echo():
    # 이 블록의 코드와 결과를 출력
    name = 'Hyunyeong An'
    st.write("hello, Streamlit!", name)

st.latex('\int_a^b f(x) dx') # 수식 입력
"$$\int_a^b f(x) dx$$" # 수식 입력

'#### :orange[이미지: st.image()]'
st.image

'#### :orange[비디오: st.video()]'
st.video

'#### :orange[오디오: st.audio()]'
st.audio

st.divider() # 구분선
'# :콜아웃'

'#### :orange[정보: st.info()]'

'#### :orange[경고: st.warning()]'
st.warning('경고 메시지입니다.', icon="⚠️")

'#### :orange[성공: st.success()]'
st.success('성공 메시지입니다.', icon="✅")

'#### :orange[Pandas 데이터프레임]'
import pandas as pd
df=pd.DataFrame(
    {'id': [1, 2, 3],
     'name': ['Alice', 'Bob', 'Charlie'],
     'age': [25, 30, 35]}
)
df # 데이터프레임 출력

st.markdown("""
|이름|학번|학과|
|---|---|---|
|홍길동|20230001|컴퓨터공학과|
|김철수|20230002|전자공학과|
|박영희|20230003|기계공학과|
""")

st.metric("Temperature", "70 °F", "1.2 °F") # 지표 출력

'#### :orange[지표(Metric)]'
col1, col2, col3 = st.columns(3) # 3개의 열 생성
col1.metric("Temperature", "70 °F", "1.2 °F")
col1.write("이것은 온도 지표입니다")
col2.metric("Wind", "9 mph", "-8%")
col3.metric("Humidity", "86%", "4%")

col3.latex('\int_a^b f(x) dx') # 수식 입력
st.divider() # 구분선
